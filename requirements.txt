# Core test and harness dependency.
pytest>=7.0

# Build backend imported by submission/setup.py.
setuptools>=68

# Used by submission/ as a numeric library only (explicitly permitted by the
# assignment: "numerical libraries (NumPy)"). No search/indexing library is
# used anywhere -- postings construction, compression, and all scoring are
# our own. NumPy is what makes the VByte codec and per-query scoring
# array-parallel instead of interpreted Python loops.
numpy>=1.24

# Builds submission/_fasttok.pyx as the C++ tokenizer extension. The
# provided Docker and grading images contain the required compiler.
Cython>=3.0

# Needed only for scripts/download_full_corpus.py (fetching the real
# assignment corpus). Not required to run the toy set or the harness itself.
ir_datasets>=0.5.5

# Needed only for harness/generate_baseline.py (regenerating the
# reference BM25 "beat this" run file). A precomputed run file is already
# shipped in data/toy/, so most students will never need to run this.
rank_bm25>=0.2.2

# Optional: if you want to cross-check this harness's metrics against the
# standard TREC tool. Not required.
# pytrec_eval>=0.5
